@model IEnumerable<CristalImb.Model.Entities.Inmueble>

@{
    ViewData["Title"] = "IndexInmueble";
}

@section Styles{
    <style>
        .contenido {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            text-align: center;
            margin-bottom: 5%;
        }

        tr td {
            font-family: 'Times New Roman', Times, serif;
            font-size: 18px;
            color: #08006f;
        }
    </style>
}

<section class="content">
    <div class="container-fluid">
        <div class="m-0 row justify-content-center align-items-baseline">
            <div class="contenido">
                <div class="botonCrear col-12 p-3 text-center accent-teal">
                    <br />
                    <p>
                        <a asp-action="RegistrarInmueble" class="btn btn-outline-success"><i class="fas fa-plus"></i> Registrar inmueble.</a>
                    </p>
                    <br />
                </div>
                <div>
                    <table class="table table-striped table-bordered col-12" id="tabla">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Codigo)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Descripcion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Tipo)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Servicio)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Valor)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Area)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Zona)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.oferta)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.RutaImagen)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Estado)
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var x in Model)
                            {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelx => x.Codigo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Descripcion)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Tipo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Servicio)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Valor)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Area)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Zona)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.oferta)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.RutaImagen)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Estado)
                                </td>
                                <td>
                                    <a asp-action="EditarInmueble" asp-route-id="@x.InmuebleId" class="btn alert-primary"><i class="fas fa-edit"></i></a>
                                    <form asp-action="EliminarInmueble" asp-route-id="@x.InmuebleId" class="d-inline">
                                        <button type="submit" onclick="confirm('¿Desea eliminar este registro?')" class="btn alert-danger"><i class="fas fa-trash-alt"></i></button>
                                    </form>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts{
    <script src="~/js/tabla.js"></script>
    <script>
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("GuardarInmueble")) { <text>
            Swal.fire({
            title: "Guardar inmueble",
            icon: 'success',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("EditarInmueble")) { <text>
            Swal.fire({
                title: "Editar inmueble",
            icon: 'success',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("Error")) { <text>
            Swal.fire({
            title: "Error",
            icon: 'error',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }

    </script>
}
