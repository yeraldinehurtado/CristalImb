@model IEnumerable<CristalImb.Model.Entities.Propietario>

@{
    ViewData["Title"] = "Propietarios";
}

@section Styles{
    <style>
        .contenido {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            text-align: center;
            margin-bottom: 5%;
        }

        tr td {
            font-family: 'Times New Roman', Times, serif;
            font-size: 18px;
            color: #08006f;
        }
    </style>
}

<section class="content">
    <div class="container-fluid">
        <div class="m-0 row justify-content-center align-items-baseline">
            <div class="contenido">
                <div class="botonCrear col-12 p-3 text-center accent-teal">
                    <br />
                    <p>
                        <a onclick="mostrarModal('@Url.Action("RegistrarPropietario","Propietario", "null", Context.Request.Scheme)', 'Registrar propietario')" class="btn btn-outline-success"><i class="fas fa-plus"></i> Registrar propietario.</a>
                    </p>
                    <br />
                </div>
                <div>
                    <table class="table table-striped table-bordered col-12" id="tabla">
                        <thead class="bg-dark text-white">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Identificacion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Nombre)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Apellido)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Telefono)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Correo)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Direccion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Inmuebles)
                                </th>
                                <th>
                                    Agregar Inmuebles
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Estado)
                                </th>
                                <th> </th>
                                <th> </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var x in Model)
                            {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelx => x.Identificacion)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Apellido)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Telefono)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Correo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelx => x.Direccion)
                                </td>
                                <td>
                                    <a onclick="mostrarModal('@Url.Action("VerInmuebles","Propietario", new { id = x.PropietarioId }, Context.Request.Scheme)', 'Ver Inmuebles')"> Ver inmuebles.</a>
                                </td>
                                <td>
                                    <a onclick="mostrarModal('@Url.Action("CrearInmPropietarios","Propietario", new { id = x.PropietarioId }, Context.Request.Scheme)', 'Crear inmpropietarios')"> Agregar inmuebles.</a>
                                </td>
                                <td>
                                    @if (x.Estado == true)
                                    {
                                        <p>Activo</p>
                                    }
                                    else
                                    {
                                        <p>Desactivo</p>
                                    }
                                </td>
                                <td>
                                    @if (x.Estado == true)
                                    {
                                        <form asp-action="ActualizarEstado" asp-route-id="@x.PropietarioId">
                                            <button type="submit"><i class="fas fa-toggle-on"></i></button>
                                        </form>
                                    }
                                    else if (x.Estado == false)
                                    {
                                        <form asp-action="ActualizarEstado" asp-route-id="@x.PropietarioId">
                                            <button type="submit"><i class="fas fa-toggle-off"></i></button>
                                        </form>
                                    }
                                </td>
                                <td>
                                    <a asp-action="EditarPropietario" asp-route-id="@x.PropietarioId" class="btn alert-primary"><i class="fas fa-edit"></i></a>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts{
    <script src="~/js/Tabla.js"></script>
    <script src="~/js/modal.js"></script>

    <script>
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("GuardarPropietario")) {
        <text>
        Swal.fire({
        title: "Guardar propietario",
            icon: 'success',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("EditarPropietario")) {
        <text>
        Swal.fire({
        title: "Editar propietario",
            icon: 'success',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }
        @if (TempData["Mensaje"] != null && TempData["Accion"].ToString().Equals("Error")) {
        <text>
        Swal.fire({
        title: "Error",
            icon: 'error',
            html: "@TempData["Mensaje"]"
            })
        </text>
        }
    </script>
}